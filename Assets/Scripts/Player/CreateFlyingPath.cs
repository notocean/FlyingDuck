using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateFlyingPath : MonoBehaviour
{
    //[SerializeField] PlayerInputEvent playerControlInput;
    //[SerializeField] List<Vector2> points = new List<Vector2>();
    //Vector2 startPos;
    //bool isFly = false;

    //public void MoveHandle(ButtonType buttonType, ButtonState buttonState) {
    //    switch (buttonState) {
    //        case ButtonState.Tap:
    //            startPos = transform.position;
    //            isFly = true;
    //            break;
    //        default:
    //            break;
    //    }
    //}

    //private void Update() {
    //    if (isFly)
    //        Record();
    //}

    //void Record() {
    //    if (transform.position.y >= startPos.y - 20) {
    //        points.Add(transform.position);
    //    }
    //}

    //private void OnEnable() {
    //    playerControlInput.Event.AddListener(MoveHandle);
    //}

    //private void OnDisable() {
    //    playerControlInput.Event.RemoveListener(MoveHandle);
    //}
}
